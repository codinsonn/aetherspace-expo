
/* --- numbersFromString ------------------------------------------------------------------------------ */
export const numbersFromString = str => {
    const regex = /[+-]?\d+(?:\.\d+)?/g;
    const matches = [];
    let match;
    while ((match = regex.exec(str))) matches.push(parseFloat(match[0]));
    return matches;
};
